! =========================================================
! Device: ASW-A1
! Role: Access Switch (Office A)
! =========================================================
! Purpose:
! - Connects end devices (AP, WLC)
! - Enforces Layer 2 security policies
! - Provides VLAN access and trunk uplinks to distribution layer
! - Implements DHCP Snooping, DAI, and Port Security
! =========================================================


! =========================
! SYSTEM CONFIGURATION
! =========================
hostname ASW-A1

! Secure privileged EXEC access
enable secret 5 <hidden>

! SSH configuration
ip domain-name jeremysitlab.com
ip ssh version 2

! DNS server
ip name-server 10.5.0.4

! Local user authentication
username cisco secret 5 <hidden>


! =========================
! LAYER 2 SECURITY FEATURES
! =========================

! Enable Dynamic ARP Inspection on Office A VLANs
ip arp inspection vlan 10,20,40,99
ip arp inspection validate src-mac dst-mac ip

! Enable DHCP Snooping on user VLANs
ip dhcp snooping vlan 10,20,40,99

! Disable option 82
no ip dhcp snooping information option

! Enable DHCP Snooping globally
ip dhcp snooping


! =========================
! DISCOVERY & STP
! =========================

! Enable LLDP for topology verification
lldp run

! Rapid STP for fast convergence
spanning-tree mode rapid-pvst
spanning-tree extend system-id


! =========================
! ACCESS PORT CONFIGURATION
! =========================

! -------- Fa0/1 → Wireless AP (Mgmt VLAN)--------
interface FastEthernet0/1

 ! Management VLAN (AP, WLC traffic)
 switchport access vlan 99

 ! Limit DHCP rate (prevents starvation attacks)
 ip dhcp snooping limit rate 15

 ! Disable LLDP transmit (reduce info exposure)
 no lldp transmit

 ! Force access mode
 switchport mode access
 switchport nonegotiate

 ! Port Security enabled
 switchport port-security

 ! Learn MAC addresses dynamically
 switchport port-security mac-address sticky

 ! Restrict (not shutdown) on violation
 switchport port-security violation restrict

 ! Predefined sticky MAC (known device)
 switchport port-security mac-address sticky 0002.4AE7.1601

 ! Edge port optimization
 spanning-tree portfast

 ! Protect from rogue switches
 spanning-tree bpduguard enable


! -------- Fa0/2 → WLC --------
interface FastEthernet0/2

 ! Trunk to support WiFi + Management VLANs
 switchport trunk native vlan 99
 switchport trunk allowed vlan 40,99

 ! Higher DHCP rate (WLC centrally switches traffic for Office A and B)
 ip dhcp snooping limit rate 100

 switchport mode trunk
 switchport nonegotiate

 ! Treat as edge trunk (faster convergence)
 spanning-tree portfast trunk

 ! Protect from rogue switches
 spanning-tree bpduguard enable


! =========================
! UNUSED ACCESS PORTS
! =========================

! Shutdown unused ports for security
interface range FastEthernet0/3 - 24
 shutdown


! =========================
! UPLINKS TO DISTRIBUTION
! =========================

! Based on LLDP:
! Gi0/1 → DSW-A1
! Gi0/2 → DSW-A2

interface GigabitEthernet0/1

 ! Trunk uplink to DSW-A1
 switchport trunk native vlan 1000
 switchport trunk allowed vlan 10,20,40,99

 ! Trust upstream infrastructure for DHCP and ARP
 ip dhcp snooping trust
 ip arp inspection trust

 switchport mode trunk
 switchport nonegotiate


interface GigabitEthernet0/2

 ! Redundant trunk uplink to DSW-A2
 switchport trunk native vlan 1000
 switchport trunk allowed vlan 10,20,40,99

 ip dhcp snooping trust
 ip arp inspection trust

 switchport mode trunk
 switchport nonegotiate


! =========================
! MANAGEMENT VLAN
! =========================

interface Vlan99
 ip address 10.0.0.4 255.255.255.240

! Default gateway (HSRP virtual IP)
ip default-gateway 10.0.0.1


! =========================
! HARDENING
! =========================

! Disable CDP (reduce attack surface)
no cdp run


! =========================
! MONITORING
! =========================

logging trap debugging
logging 10.5.0.4

snmp-server community SNMPSTRING RO


! =========================
! REMOTE ACCESS CONTROL
! =========================

! Restrict management access
access-list 1 permit 10.1.0.0 0.0.0.255

line vty 0 4
 access-class 1 in
 logging synchronous
 login local
 transport input ssh

line vty 5 15
 access-class 1 in
 logging synchronous
 login local
 transport input ssh


! =========================
! CONSOLE ACCESS
! =========================

line con 0
 logging synchronous
 login local
 exec-timeout 30 0


! =========================
! TIME SYNCHRONIZATION
! =========================

ntp trusted-key 1
ntp server 10.0.0.76 key 1
